Left-sidebar navigation button. `active` fills it solid accent-blue with white text; idle items are transparent and tint soft-blue on hover. Full-width, left-aligned, 16px/700.

```jsx
<NavItem active>同步任务</NavItem>
<NavItem>同步日志</NavItem>
<NavItem>设置</NavItem>
```
